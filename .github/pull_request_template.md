## Summary

Briefly describe what this PR changes and why.

## Changes

-
-

## Validation

- [ ] Java tests pass (`./mvnw test`)
- [ ] Go tests pass (`go test ./...`) if simulator code changed
- [ ] Go race detector passes (`go test -race ./...`) if concurrent simulator code changed
- [ ] No existing behavior was unintentionally changed

## Database

- [ ] No database schema change
- [ ] New Flyway migration added if the schema changed
- [ ] Existing applied Flyway migrations were not modified

## Notes

Add any architecture decisions, trade-offs, screenshots, logs, or reviewer context that is useful.
